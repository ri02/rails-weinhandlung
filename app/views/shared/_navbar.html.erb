<div class="nav">
 <div class= "nav-list">
  <p class="nav-list-home">HOME</p>
  <% super_categories.each do |super_category| %>
    <%= button_to "#{super_category.name.upcase}", products_path, method: :get, params: {products: {super_category: super_category.name}}, class: "nav-list-item" %>
  <% end %>
  <p class="nav-list-item">WARENKORB</p>
  <span class="nav-list-burger"><i id="burger-icon" class="fas fa-bars"></i></span>
 </div>
</div>

<div class="sidebar-box">
  <div class="sidebar-grey"></div>
  <div class="sidebar">
    <div class= "sidebar-list">
     <p class="sidebar-list-home">HOME <i id="arrow-icon" class="fas fa-chevron-right"></i></p>
     <% super_categories.each do |super_category| %>
       <%= button_to "#{super_category.name.upcase}", products_path, method: :get, params: {products: {super_category: super_category.name}}, class: "sidebar-list-item" %>
     <% end %>
     <p class="sidebar-list-item">WARENKORB</p>
    </div>
    <div class="sidebar-footer">

    </div>
  </div>

</div>





